TODO:
Don't create file duplicates
uploading files to cloud should automatically create folders
rename
clouds_manager -> Manage clouds
cache files from cloud (at least last file for each session)
Display the user's clouds nicely
don't use session_id to identify the user, instead use accounts/username/, this way you can do sharing
